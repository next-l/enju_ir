<%= form_with(model: fileset, local: true) do |form| %>
  <% if fileset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fileset.errors.count, "error") %> prohibited this fileset from being saved:</h2>

      <ul>
      <% fileset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
